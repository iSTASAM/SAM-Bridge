PUSH_API_KEY=
AUTH_USER=admin
AUTH_PASSWORD=
# Extra admins are created in /settings/systems/admins and sign in at /admin/login.

# Optional: overrides the GPT Actions key managed from /settings/gpt-actions
GPT_ACTION_API_KEY=

# Supabase — stores iXacs connections, Push API keys, LINE webhook settings, export configs,
# AI provider credentials, the default AI model, and extra admin accounts.
# Required on Vercel — local JSON is not durable.
# Push event payloads stay local (andon-state) — not written to Supabase.
# Project Settings → API
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# AES key for customer ID / login ID / password / AI API keys at rest (DB + local file).
# Use a long random string. Falls back to AUTH_PASSWORD if unset (not recommended).
# Required on Vercel so AI keys are never stored in plaintext.
CONNECTIONS_ENCRYPTION_KEY=

# LINE Messaging API / LIFF (required on Vercel for webhook Verify)
# Messaging API → Channel secret
LINE_CHANNEL_SECRET=
# Required for per-user Rich Menu switching after /line login.
# May instead be saved securely in /settings/notifications/line-webhook.
LINE_CHANNEL_ACCESS_TOKEN=
# Rich Menu 2 shown after a successful iXacs login.
LINE_RICHMENU_LOGGED_IN=richmenu-ab29249ac133a6fa9f2b9393f9a82d95
# Optional: restore this default (login) rich menu on logout
# LINE_RICHMENU_LOGGED_OUT=richmenu-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Public HTTPS URL for LINE webhooks, GPT Actions, and Excel/Power BI export queries.
# Dev: leave as production URL, or open the export wizard on http://localhost:4525
#      so Power Query uses localhost automatically.
LINE_PUBLIC_URL=https://sam-bridge.vercel.app
LINE_LIFF_ID=
LINE_LOGIN_CHANNEL_ID=

# Optional: Bearer token for GET /api/notifications/monitor (Supabase pg_cron fallback only).
# Instant alerts use iXacs Push → /api/push. Do not use Vercel Cron (Hobby rejects sub-daily).
# Local/ngrok: instrumentation starts an in-process monitor loop (default 5000ms).
# NOTIFICATION_MONITOR_MS=5000
CRON_SECRET=

# Optional Slack fallbacks. Admin can store these encrypted from /settings/systems/slack.
SLACK_INCOMING_WEBHOOK=
SLACK_CHANNEL_ID=
SLACK_BOT_TOKEN=
SLACK_SIGNING_SECRET=

# Allow /line login + dashboard in a normal browser for UX review
# (auto-on in development; set to 1 on Vercel if needed)
LINE_ALLOW_WEB_PREVIEW=1

# Optional LINE AI abuse controls. Defaults: 5 requests/minute and 30 requests/hour per LINE user.
LINE_AI_RATE_LIMIT_PER_MINUTE=5
LINE_AI_RATE_LIMIT_PER_HOUR=30

# Optional: USD→THB rate for estimated AI spend (default 35)
# AI_USD_TO_THB=35
